FROM petenorth/graal:1.0.0-rc5
RUN apt-get update && apt-get -y install gcc zlib1g-dev
COPY target /app/
RUN ls 
WORKDIR /app
RUN ls
RUN native-image \
     --no-server \
     -H:+ReportUnsupportedElementsAtRuntime \
     --static \
     -jar target/$(ls target/*.jar)
     
